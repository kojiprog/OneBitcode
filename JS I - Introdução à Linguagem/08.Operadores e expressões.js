// Aritméticos
// Soma (+), subtração (-), multiplicação (*), divisão (/), resto da divisão (%)
// Precedência: *, / têm precedência sobre +, -
// Uso de parênteses para agrupamento

// Atribuição
// Atribuição simples (=)
// Atribuição com operação (+=, -=, *=, /=)

// Encadeamento
// Ponto (.) para encadear objetos e funções

// Incremento e decremento
// Incremento (++), decremento (--)
// Posição altera valor retornado pela expressão